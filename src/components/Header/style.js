import { makeStyles } from '@material-ui/styles'

const styles = makeStyles(theme => ({
  header: {
    width: '100%',
    height: '10vh',
    borderBottom: '1px solid #ef9610',
    padding: '0 5%',
    backgroundColor: '#242424',
    color: '#ef9610'
  },
  logo: {
    color: '#ef9610'
  }
}))

export default styles
